version: '3'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DEBUG=1
    depends_on:
      - db

  db:
    image: mysql:8
    environment:
      MYSQL_DATABASE: book_db
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
